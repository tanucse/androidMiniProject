<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.androidassignment">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".main"></activity>
        <activity android:name=".RadiobuttonDemo" />
        <activity android:name=".MusicActivity" />
        <activity android:name=".SongList" />
        <activity android:name=".Quiz" />
        <activity android:name=".Result" />
        <activity android:name=".Question" />
        <activity android:name=".ShowData" />
        <activity android:name=".MyMenu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GameWord" />
        <activity android:name=".JumbleWordGame" />
        <activity android:name=".Calculator" />
        <activity android:name=".TicTacToe" />
        <activity android:name=".StudentManagement" />
    </application>

</manifest>